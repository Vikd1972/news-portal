const requiredErrorMessage = 'Поле должно быть заполненно';

const localPath = {
  news: '/',
  writeNews: '/write-news',
  signIn: '/sign-in',
  signUn: '/sign-up',
  profile: '/profile',
};

export default {
  requiredErrorMessage,
  localPath,
};
